# Название проекта

Проект для автоматизации управления данными о скважинах и объектах в нефтедобывающей отрасли.

## Описание

Проект включает в себя серверное приложение на языке Go, предоставляющее REST API для выполнения операций CRUD (создание, чтение, обновление, удаление) с данными о скважинах, объектах, историях скважин за день и планах скважин за день. Для хранения данных используется PostgreSQL. Документация API сгенерирована с помощью Swagger.

## Основные компоненты

- **Backend на Go**: Реализация логики обработки запросов к базе данных PostgreSQL.
- **REST API**: Стандартизированный интерфейс для работы с данными о скважинах и объектах через HTTP запросы.
- **PostgreSQL**: Хранение и управление данными о скважинах, объектах, историях и планах скважин.
- **Swagger**: Автоматическая генерация документации API для упрощения использования разработчиками.
